% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_low_expression.R
\name{filter_low_expression}
\alias{filter_low_expression}
\title{Filter low-expression genes from a count matrix}
\usage{
filter_low_expression(
  counts,
  min_count = 10,
  min_cpm = 1,
  min_prop = 0.15,
  verbose = TRUE
)
}
\arguments{
\item{counts}{A numeric matrix of raw counts (genes x samples) or a
\code{SummarizedExperiment} object.}

\item{min_count}{Numeric. Minimum raw count threshold (default 10).}

\item{min_cpm}{Numeric. Minimum CPM threshold (default 1).}

\item{min_prop}{Numeric. Minimum proportion of samples that must exceed both
thresholds (default 0.15).}

\item{verbose}{Logical. Print filtering summary (default TRUE).}
}
\value{
A numeric matrix of filtered counts (genes x samples).
}
\description{
Removes genes that are not expressed above specified thresholds in a
sufficient proportion of samples. Applies two filters: one on raw counts
and one on CPM values. Only genes passing both filters are retained.
}
\details{
This function replicates the filtering strategy used in the CATaN pipeline:
\enumerate{
\item Compute CPM using \code{edgeR::cpm()}.
\item Identify genes where at least \code{min_prop} of samples have
raw count >= \code{min_count}.
\item Identify genes where at least \code{min_prop} of samples have
CPM >= \code{min_cpm}.
\item Retain genes passing both criteria.
}
}
\examples{
set.seed(1)
counts <- matrix(rnbinom(1000, mu = 10, size = 1), nrow = 100, ncol = 10)
rownames(counts) <- paste0("gene", seq_len(100))
filtered <- filter_low_expression(counts)

}
