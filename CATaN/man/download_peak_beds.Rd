% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data.R
\name{download_peak_beds}
\alias{download_peak_beds}
\title{Download TF peak BED files from Zenodo}
\usage{
download_peak_beds(dest_dir, url = NULL, cache = TRUE, verbose = TRUE)
}
\arguments{
\item{dest_dir}{Character. Directory to extract BED files into. Created
if it does not exist.}

\item{url}{Character. URL of the tar.gz archive. If NULL (default), uses
the default Zenodo URL.}

\item{cache}{Logical. Whether to use BiocFileCache for caching
(default TRUE).}

\item{verbose}{Logical (default TRUE).}
}
\value{
Character. Path to the directory containing the extracted BED files.
}
\description{
Downloads the TF ChIP-seq peak BED files (hg19) used in the CATaN paper.
The archive is cached via BiocFileCache and extracted to the specified
directory.
}
\details{
On first call, the archive (~1 GB) is downloaded and cached. Subsequent
calls skip the download and use the cached copy. The archive is extracted
to \code{dest_dir}, which can then be passed to
\code{\link{align_cc_to_snps}} as the \code{peak_dir} argument.
}
\examples{
\donttest{
peak_dir <- download_peak_beds("~/catan_data/peaks")
}

}
